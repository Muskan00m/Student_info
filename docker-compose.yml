# version: "3.9"

services:
  web:
    build: .
    command: gunicorn infoManagement.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - redis
    env_file:
      - .env

  redis:
    image: redis:7
    ports:
      - "6379:6379"